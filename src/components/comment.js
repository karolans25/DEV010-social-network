export const comment = (navigateTo, idPub) => {
  const sectionCommentsPost = document.createElement('section');
  const title = document.createElement('h2');

  title.textContent = 'Post';
  console.log(idPub);

  return sectionCommentsPost;
};
